// TODO: Remove this when the implementation is reasonably complete and there
// are no more unused parts.
#![allow(dead_code)]
mod let_expr;
mod let_normalize;
mod result;
mod simple_eval;
mod syntax;

fn main() {
    println!("Hello, world!");
}
